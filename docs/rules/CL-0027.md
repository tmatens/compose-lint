# CL-0027: Bounded-grant capabilities (SYS_PTRACE, PERFMON, SYS_TIME, DAC_READ_SEARCH)

**Severity:** MEDIUM

**Derivation** (see [severity model](../severity.md)):

- **Baseline:** A — the attacker already has code execution in this container, as the workload uid
- **Precondition:** Second flaw — each member's grant converts into impact only where the image supplies something this file cannot see: a process running as a *different* uid for `SYS_PTRACE` to trace, or a file the workload uid cannot already read for `DAC_READ_SEARCH`. Same-uid tracing and same-uid reads need no capability at all. Parallel to [CL-0003](CL-0003.md), whose named primitive is likewise a property of the image
- **Impact:** Single container — priced at `SYS_PTRACE`, whose grant is read *and write* access to another process's memory inside this container. `DAC_READ_SEARCH`'s host leg reaches further but only where a host bind mount is already present — a sibling the model scores in isolation and [CL-0013](CL-0013.md)/[CL-0025](CL-0025.md) flag separately
- **Qualifier/modifier:** none — `SYS_PTRACE` writes process memory, so `read-only` does not apply. It would fit `DAC_READ_SEARCH` alone, which is not the pricing member
- **Derived:** Second flaw × Single container = MEDIUM
- **Shipped:** MEDIUM
- **Scoping assumptions:** `SYS_TIME` is the member the impact axis fits worst: its reach *is* host-wide (the clock is not namespaced) but it corrupts host time integrity, which the matrix has no cell for. `PERFMON` reaches kernel-wide reads at the grounded kernel default — measured, see Evidence — but is inert on the *hardened* host the premise suite runs against. Neither is the pricing member, so the cell does not rest on either
- **Daemon assumptions:** grounded at the **upstream kernel defaults** `kernel.perf_event_paranoid = 2` and `kernel.yama.ptrace_scope = 0` (ADR-020). Debian and Ubuntu ship `perf_event_paranoid = 3`, which requires `CAP_SYS_ADMIN` and leaves `PERFMON` inert; Arch and Ubuntu ship `ptrace_scope = 1`, which narrows `SYS_PTRACE`'s *same*-uid reach. Neither moves the cell: it is priced on `SYS_PTRACE`'s cross-uid grant, which no `ptrace_scope` value affects
- **Evidence:** `_cl0011` in `scripts/validate_rule_premises.py` proves only that `cap_add` deposits the capability into the effective set — not what any member grants. The pricing member is checked by `_t_sys_ptrace`: with `--cap-drop ALL --cap-add SETUID,SETGID`, `PTRACE_ATTACH` to a different-uid process in the same container is refused (`Operation not permitted`), and adding `SYS_PTRACE` attaches. The probe crosses a uid boundary deliberately — that test is `ptrace_may_access`'s capability check and does not depend on `kernel.yama.ptrace_scope`, whereas the *same*-uid case does and is not the grant (it succeeds with every capability dropped). `_t_dac_read_search` covers the second member: a mode-000, other-owned file is unreadable at `--cap-drop ALL` and readable with `DAC_READ_SEARCH`. Both run against a live daemon on every CI run. `SYS_TIME` is checked by `_t_sys_time`, which confirms the **grant**: a `clock_gettime`→`clock_settime` round-trip of the same timespec is refused without the capability and succeeds with it. That the affected clock is the *host's* follows from `CLOCK_REALTIME` not being namespaced; it is reasoned, not measured — the round-trip deliberately changes nothing. `PERFMON` is checked by `_t_perfmon`, which asserts the grounded behaviour: `--cap-drop ALL` is refused a system-wide `perf_event_open` (`pid=-1, cpu=0`, kernel samples included) and `--cap-drop ALL --cap-add PERFMON` opens one — a read covering host processes, with no sibling key. Upstream, `perfmon_capable()` lets the capability bypass `perf_event_paranoid` at any level. Some distributions patch that: Debian 13 at `perf_event_paranoid=3` refuses even with the capability, because the patched check demands `CAP_SYS_ADMIN`. The sysctl value alone does not predict which behaviour you get — a CI runner at **4** grants, Debian at **3** refuses, Arch at **2** grants — so a failure of this check means the daemon's host carries that hardening, not that the reach is in doubt

## What it detects

`cap_add` entries whose grant is real but bounded:

| Capability | Grants | Why it is not higher |
|---|---|---|
| `SYS_PTRACE` | trace and read other processes' memory | confined to this container's own PID namespace unless `pid: host` is also set, which [CL-0010](CL-0010.md) flags |
| `PERFMON` | `perf_event_open` — kernel timing and info disclosure | a read primitive; it discloses, it does not write |
| `SYS_TIME` | set the system clock | host-global (Docker does not isolate `CLOCK_REALTIME`), so it breaks certificate validation, TOTP and Kerberos host-wide — but it is an integrity effect, not a takeover |
| `DAC_READ_SEARCH` | bypass file-read checks; read host files via `open_by_handle_at` | the host-read leg needs a host bind mount to be present, and that mount is flagged by [CL-0013](CL-0013.md) or [CL-0025](CL-0025.md) |

## Why it matters

These weaken isolation without opening an escape. That distinction is the whole
point of the tier: before the split they were graded HIGH alongside kernel-module
loading, which made the higher tiers less believable and put a debugger sidecar
in the same bucket as a container escape.

`SYS_TIME` is the one most often underestimated. The clock is **host-global** —
a container holding this capability moves the host's wall clock, and every
workload on the machine sees it. Certificate validation, time-based one-time
passwords and Kerberos all break together, and the failure looks like an outage
rather than an attack.

## Fix

Remove the capability unless the workload demonstrably needs it:

```yaml
# Before
services:
  profiler:
    image: myprofiler:1.0
    cap_add:
      - SYS_PTRACE

# After — if the sidecar genuinely profiles a neighbour, scope it explicitly
services:
  profiler:
    image: myprofiler:1.0
    cap_drop:
      - ALL
    cap_add:
      - SYS_PTRACE
    pid: "service:app"     # not pid: host — see CL-0010
```

## When to suppress

These are the capabilities with the most legitimate uses in the set, so
suppression here is ordinary rather than exceptional:

- `SYS_TIME` — an NTP client (chrony, ntpd) running in a container. Prefer
  syncing the clock on the *host* and letting containers inherit it; a container
  that sets the host clock is a design smell even when it works.
- `SYS_PTRACE` / `PERFMON` — debugger, profiler and APM sidecars. Scope them to
  the container they inspect with `pid: "service:<name>"` rather than `pid: host`.

Suppress per service with a `reason:` naming the workload.

## ATT&CK coverage

Remediating this finding contributes to mitigating the following MITRE ATT&CK techniques (pinned to ATT&CK v18). compose-lint is a static analyser, so this is *mitigation* coverage — it detects nothing at runtime.

| Technique | Tactic |
|---|---|
| [T1055 Process Injection](https://attack.mitre.org/techniques/T1055/) | Privilege Escalation |
| [T1003 OS Credential Dumping](https://attack.mitre.org/techniques/T1003/) | Credential Access |
| [T1552.001 Unsecured Credentials: Credentials In Files](https://attack.mitre.org/techniques/T1552/001/) | Credential Access |
| [T1611 Escape to Host](https://attack.mitre.org/techniques/T1611/) | Privilege Escalation |
| [T1070 Indicator Removal](https://attack.mitre.org/techniques/T1070/) | Stealth |

## See also

- [CL-0024](CL-0024.md) — host-code-execution capabilities (CRITICAL)
- [CL-0011](CL-0011.md) — strong host-adjacent capabilities (HIGH)
- [CL-0010](CL-0010.md) — `pid: host`, which removes `SYS_PTRACE`'s namespace confinement
